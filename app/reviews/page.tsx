import type { Metadata } from "next"; import { Shell } from "@/components/Shell"; import { PageHero } from "@/components/PageHero"; import { CTA } from "@/components/CTA"; import { reviews } from "@/data/reviews";
export const metadata:Metadata={title:"Customer Reviews",description:"Customer review placeholders for TrueFit Home Improvements, serving Sumter and Lake Counties, Florida."};
export default function Reviews(){return <Shell><PageHero eyebrow="REVIEWS" title="Thoughtful work. Happy homes.">Authentic customer feedback belongs here. The entries below are deliberately marked as placeholders until approved reviews are supplied.</PageHero><section className="section"><div className="container review-grid review-page">{reviews.map((r,i)=><blockquote key={i}><div className="stars">★★★★★</div><p>“{r.quote}”</p><footer><b>{r.name}</b><span>{r.location}</span></footer></blockquote>)}</div><div className="container review-link-box"><h2>Have a Google review link?</h2><p>Replace this placeholder with the business’s verified Google review URL after the profile is ready.</p><a className="button button-navy" href="#">Google review link placeholder</a></div></section><CTA/></Shell>}
