import type { Metadata } from "next"; import Link from "next/link"; import { Shell } from "@/components/Shell"; import { PageHero } from "@/components/PageHero"; import { CTA } from "@/components/CTA"; import { Check, Arrow } from "@/components/icons"; import { services } from "@/data/services";
export const metadata:Metadata={title:"Handyman & Small Home Improvement Services",description:"Explore handyman, trim, interior door, painting, wallpaper, LVP flooring, drywall, siding repair and fixture services in Sumter and Lake Counties."};
export default function Services(){return <Shell><PageHero eyebrow="SERVICES" title="Helpful solutions for the projects on your list.">Repairs, updates, and finishing touches—handled with a practical plan and a detail-minded approach.</PageHero><section className="section"><div className="container service-detail-list">{services.map((s,i)=><article id={s.slug} key={s.slug}><div className="service-number">0{i+1}</div><div><h2>{s.title}</h2><p>{s.description}</p><ul>{s.examples.map(e=><li key={e}><Check/>{e}</li>)}</ul></div><Link className="text-link dark" href={`/request-a-quote?project=${s.slug}`}>Ask about this service <Arrow/></Link></article>)}</div></section><section className="notice"><div className="container"><b>Not sure if your project fits?</b><p>Send a few details. We’ll review the request and let you know whether it’s within our service scope.</p></div></section><CTA/></Shell>}
